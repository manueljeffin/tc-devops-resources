apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: tc-devops
  region: ap-south-1
  version: "1.22"

nodeGroups:
  - name: tc-ng-1
    instanceType: m5.large
    desiredCapacity: 1
    volumeSize: 80